# Objective

No task content has been provided. The request is empty, so there is no engineering objective to plan against. A planning document cannot be produced without a concrete requirement or change request.

# Analysis

- The task input is blank: no user requirement, bug report, feature ask, or file reference has been supplied.
- No repo context was loaded beyond the branch name (`refs/heads/main`) and an empty `CLAUDE.md`.
- There is no actionable signal to ground a plan in: no target file, no desired behavior change, no acceptance signal.
- Producing speculative tasks here would violate the engineering principles (no speculative features, no assumptions beyond context).

# Tasks

None. No implementation tasks can be authored without a defined requirement.

ID: task000
Type: implementation
Description: Awaiting a concrete requirement from the requester before any file change is planned.
Acceptance Criteria:
- The requester provides a specific change, bug, or feature to implement.
- Scope and target files are identifiable from the provided context.
Validation:
- Re-run planning once the requirement and relevant repo files are available.

# Risks

- Fabricating tasks without a real requirement would lead to unjustified code changes.
- Acting on an empty prompt risks modifying unrelated files or introducing speculative features, both of which are explicitly out of scope.